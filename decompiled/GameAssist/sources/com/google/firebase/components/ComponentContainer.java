package com.google.firebase.components;

import com.google.firebase.inject.Provider;
import java.util.Set;

/* loaded from: classes.dex */
public interface ComponentContainer {
    Object a(Class cls);

    Provider b(Class cls);

    Set c(Class cls);

    Provider d(Class cls);
}
